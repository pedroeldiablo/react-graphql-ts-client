query Posts($limit: Int!, $cursor: String) {
  posts(cursor: $cursor, limit: $limit) {
    hasMore
    posts {
      id
    title
    text
    textSnippet
    createdAt
    updatedAt
    creatorId 
    creator {
        id
        username
      }
    }  
  }
}
